@model IEnumerable<WebApplication1.Atendente>

@{
    ViewData["Title"] = "Index";
}

<p>
    <a asp-action="Create">Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Email
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AtePessoa.PesEmail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AteId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.AteId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.AteId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
