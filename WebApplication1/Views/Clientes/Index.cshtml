@model IEnumerable<WebApplication1.Cliente>

@{
    ViewData["Title"] = "Index";
}

<p>
    <a asp-action="Create">Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Organização
            </th>
            <th>
                Email
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CliOrganizacao.OrgCnpj)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CliPessoa.PesEmail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CliId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.CliId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.CliId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
