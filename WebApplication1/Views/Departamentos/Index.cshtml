@model IEnumerable<WebApplication1.Departamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DepNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DepEmail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DepNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DepEmail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DepId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DepId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DepId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
